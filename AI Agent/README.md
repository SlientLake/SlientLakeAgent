# AI Agent

本目录用于整理 SilentLake 的 AI Agent 相关运行资产、扩展机制和代理协作文档。

当前映射：

- `extensions -> ../extensions`
- `.agents -> ../.agents`
- `.pi -> ../.pi`
- `docs.acp.md`

说明：

- Agent 相关真实资产仍在根目录对应位置，本目录用于统一索引和治理。
- `docs.acp.md` 已迁移至本目录，根目录保留兼容入口文件，避免旧路径失效。
- 涉及 agent 行为、权限、插件、子代理和运行协议的变更，应同时写清用户表现与后端机制。
