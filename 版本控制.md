# 版本控制

## 当前对外版本

- `package.json` 当前版本：`2026.4.19`

## 本地未发版变更

### 2026.4.19-local

变更类型：

- 工程结构标准化
- Gateway 与渠道主干问题修复
- UI 品牌与交互收口
- npm 打包链路与发布产物整理

版本判定：

- 本次对外版本定为 `2026.4.19`
- 延续仓库现有日期式 semver 版本体系
- 本轮包含仓库结构整理、品牌统一、主干回归修复与发布链路收口，因此相对 `2026.3.24` 进行新一轮发版收口

影响范围：

- 仓库治理结构
- 文档入口与目录索引
- `gateway / sessions / auth / channels / extensions / ui` 主干逻辑
- CLI 启动、品牌文案、帮助入口与更新链路
- `dist` 产物与 npm 打包内容

已完成内容：

- 新增标准目录：
  `设计`、`前端`、`后端`、`AI Agent`、`SKILLS`、`计算引擎`、`需求分析`、`代码仓`
- 恢复并保留根目录兼容文档入口，避免迁移后旧路径失效
- SilentLake CLI 无参数启动退出码修复，帮助与 banner 正常展示
- CLI / 登录页 / 关键 UI 品牌文案统一到 SilentLake，减少 OpenClaw 残留观感
- `sessions` / `auth` / 配对审批 / canvas / send 幂等 / plugin runtime / reload / talk / e2e 主干回归修复完成
- `ui` / `channels` / `extensions` 全量回归通过
- `feishu docx` 测试结构警告清理，避免未来 Vitest 升级后变成 error
- npm 包重新包含完整 UI 与 dist 产物，并排除测试截图和 `vitest` 附件污染
- `pnpm build`、`pnpm pack`、CLI 启动与主干测试链路已完成验证

待完成内容：

- 无

说明：

- 本文件记录的是本地工程整理与未发版变更状态，不等同于 npm 已发布版本。
