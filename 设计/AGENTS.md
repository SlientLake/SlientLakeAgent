# 设计目录约束

本目录用于存放 SilentLake 的产品设计、定位、愿景和方案性文档。

约束：

- 本目录遵循根目录 `AGENTS.md` 的全部规则。
- 本目录只承载设计与说明材料，不直接承载运行时代码。
- 设计文档变更时，应明确说明对应的前端表现、后端实现影响和验收方式。
- `AGENTS.md` 与 `CLAUDE.md` 必须保持完全一致。

当前索引：

- `SILENTLAKE.md`
- `VISION.md`
