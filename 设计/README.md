# 设计

本目录存放 SilentLake 的产品定位、设计理念和架构愿景文档。

当前内容：

- `SILENTLAKE.md`：SilentLake 的产品定位、目标和整体方向。
- `VISION.md`：长期愿景、设计原则和演进方向。

说明：

- 这里承载设计与产品层材料，不直接承载可运行代码。
- 根目录保留构建与发布入口，避免破坏现有 npm、GitHub、CI 和脚本链路。
- 若设计方案发生调整，应同步更新本目录下的 `AGENTS.md` 与 `CLAUDE.md`，两者内容必须保持一致。
