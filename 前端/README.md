# 前端

本目录是前端资产的导航层，用于把前端代码、静态资源和前端治理说明收拢到统一位置。

当前映射：

- `ui -> ../ui`
- `assets -> ../assets`

说明：

- 实际可运行前端代码仍位于根目录 `ui/`，这里通过软链接进行结构化映射。
- 这样处理可以满足工程目录规范，同时不破坏现有构建、打包和发布路径。
- 前端功能变更应同时说明用户可见交互、页面状态和对应的数据/接口影响。
