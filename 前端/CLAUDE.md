# 前端目录约束

本目录用于组织 SilentLake 的前端资产入口和前端治理说明。

约束：
- 本目录遵循根目录 `AGENTS.md` 的全部规则。
- 本目录中的软链接仅做结构映射，不改变真实源码与构建入口。
- 前端问题分析必须同时覆盖用户交互、页面逻辑和后端数据流影响。
- `AGENTS.md` 与 `CLAUDE.md` 必须保持完全一致。

当前索引：
- `ui -> ../ui`
- `assets -> ../assets`
