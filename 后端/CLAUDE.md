# 后端目录约束

本目录用于组织 SilentLake 的后端实现入口、测试入口和脚本入口。

约束：
- 本目录遵循根目录 `AGENTS.md` 的全部规则。
- 本目录中的软链接仅用于结构映射，不替代根目录真实源码路径。
- 分析和修复后端问题时，应说明功能表现、实现机制、上下游关系和验证方式。
- `AGENTS.md` 与 `CLAUDE.md` 必须保持完全一致。

当前索引：
- `src -> ../src`
- `test -> ../test`
- `scripts -> ../scripts`
