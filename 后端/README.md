# 后端

本目录是后端与测试资产的导航层，用于集中说明后端代码、测试代码和脚本入口。

当前映射：

- `src -> ../src`
- `test -> ../test`
- `scripts -> ../scripts`

说明：

- 实际后端实现仍位于根目录 `src/`、`test/`、`scripts/`。
- 本目录的职责是把后端入口标准化，而不是迁移构建关键路径。
- 后端问题说明应覆盖接口关系、数据流、状态管理、权限边界和失败原因。
